<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="Text">
        <item name="android:layout_marginTop">@dimen/spacing_small_large</item>
        <item name="android:layout_marginStart">@dimen/spacing_small_large</item>
        <item name="android:layout_marginEnd">@dimen/spacing_small_large</item>
    </style>
    <style name="Button">
        <item name="android:layout_marginTop">@dimen/spacing_small_large</item>
        <item name="android:layout_marginStart">@dimen/spacing_small_large</item>
        <item name="android:layout_marginEnd">@dimen/spacing_small_large</item>
    </style>

    <style name="ParentContainer">
        <item name="layout_constraintEnd_toEndOf">parent</item>
        <item name="layout_constraintStart_toStartOf">parent</item>
        <item name="layout_constraintTop_toTopOf">parent</item>
    </style>

</resources>
